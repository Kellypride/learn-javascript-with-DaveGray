//Numbers


//An integer is a whole number

const myNumber = 48;

//A number with a decimal piont is a float which references to the "floating point"
 const myString ="48.12323abc"
const myFlaot = 48.0;
//A string can be converted to number usng the Number() function.

//The NUmber.isInteger() method determines whether the value passed is a an integer
// The NUmber.parseFlaot() method parses an arguement and return a flaoting point number. if a number can't be parsed from the arguement it returns NAN(Not A Number)
//the toFixed() methodfromats the number according to how many decimal points you provide as the parameter
//The toString( method returns a strin representing numbers)
// NAN()
 console.log(myFlaot .toFixed(myString));